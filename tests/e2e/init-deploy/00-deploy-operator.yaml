apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 10
commands:
- script: |
    set -o errexit
    set -o xtrace

    REPO_ROOT=$(realpath ../../..)
    source "${REPO_ROOT}/hack/functions"

    deploy_operator

apiVersion: v1
kind: Pod
metadata:
  name: mysql-client
  labels:
    name: mysql-client
spec:
  containers:
  - name: mysql-client
    image: percona/percona-server:8.3
    command:
    - sleep
    - infinity

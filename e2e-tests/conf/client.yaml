apiVersion: v1
kind: Pod
metadata:
  name: mysql-client
  labels:
    name: mysql-client
spec:
  terminationGracePeriodSeconds: 10
  containers:
  - name: mysql-client
    image: percona/percona-server:8.0.33
    command:
    - sleep
    - infinity

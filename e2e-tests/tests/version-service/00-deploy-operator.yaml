apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 10
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      deploy_operator
      deploy_cluster_secrets all
      deploy_client
      deploy_version_service

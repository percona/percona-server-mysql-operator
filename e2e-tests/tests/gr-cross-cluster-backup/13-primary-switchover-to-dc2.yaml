apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 30
    script: |-
      set -o errexit
      set -o xtrace

      kubectl patch -n "${NAMESPACE}" ps-clusterset gr-cross-cluster --type=merge -p '{"spec":{"primaryCluster":"grcrossclusterbackupdc2"}}'

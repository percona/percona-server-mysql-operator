apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 120
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      apply_crd
      apply_rbac
      deploy_operator
      wait_deployment percona-server-mysql-operator ${OPERATOR_NS:-$NAMESPACE}

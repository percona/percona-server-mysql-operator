apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 300
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      get_cr "no-resources" \
          | kubectl -n "${NAMESPACE}" delete -f -

apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 150
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      renew_certificate "gr-tls-cert-manager-ssl"

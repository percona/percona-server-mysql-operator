apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 150
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      renew_certificate "gr-tls-cert-manager-ssl"

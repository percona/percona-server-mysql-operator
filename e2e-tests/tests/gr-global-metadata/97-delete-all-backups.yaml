apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 100
    script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      kubectl delete ps-backup --all -n "${NAMESPACE}"

apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 100
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      kubectl delete ps-backup --all -n "${NAMESPACE}"

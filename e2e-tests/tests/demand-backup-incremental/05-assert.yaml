apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 300
resourceRefs:
- apiVersion: ps.percona.com/v1
  kind: PerconaServerMySQLBackup
  name: base-backup
  ref: bcp
assertAll:
- celExpr: bcp.status.state == "Succeeded"
  message: "Base backup should be succeeded"

- celExpr: has(bcp.status.toLsn) && bcp.status.toLsn != ""
  message: "Base backup should have a toLsn"

- celExpr: bcp.status.type == "full"
  message: "Base backup should be a full backup"

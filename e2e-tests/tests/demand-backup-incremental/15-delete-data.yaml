apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      run_mysql \
          "TRUNCATE TABLE myDB.myTable" \
          "-h $(get_haproxy_svc $(get_cluster_name))"
      sleep 5

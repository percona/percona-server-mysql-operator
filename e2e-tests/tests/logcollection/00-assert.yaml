apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
commands:
  - script: |-
      null
      ---
      null
      ---
      kubectl assert exist-enhanced deployment percona-server-mysql-operator -n ${OPERATOR_NS:-$NAMESPACE} --field-selector status.readyReplicas=1
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: perconaservermysqls.ps.percona.com
spec:
  group: ps.percona.com
  names:
    kind: PerconaServerMySQL
    listKind: PerconaServerMySQLList
    plural: perconaservermysqls
    shortNames:
      - ps
    singular: perconaservermysql
  scope: Namespaced
commands:
  - script: |-
      null
      ---
      null
      ---
      kubectl assert exist-enhanced deployment percona-server-mysql-operator -n ${OPERATOR_NS:-$NAMESPACE} --field-selector status.readyReplicas=1
---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
metadata:
  name: check-operator-deploy-status
timeout: 120
commands:
  - script: |-
      null
      ---
      null
      ---
      kubectl assert exist-enhanced deployment percona-server-mysql-operator -n ${OPERATOR_NS:-$NAMESPACE} --field-selector status.readyReplicas=1

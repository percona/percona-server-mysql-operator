apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 420
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions
      
      bcp_count=$(count_backups_in_schedule "full" "$(get_cluster_name)")
      if [[ ! $bcp_count -eq 2 ]]; then
        echo "There are $bcp_count full ps-backups but should be 2"
        exit 1
      fi
    timeout: 420
---
kind: PerconaServerMySQLBackup
apiVersion: ps.percona.com/v1
metadata:
  labels:
    percona.com/backup-type: cron
    percona.com/cluster: gr-scheduled-backup-incremental
status:
  type: full
  state: Succeeded
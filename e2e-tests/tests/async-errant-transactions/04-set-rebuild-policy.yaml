apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 30
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      kubectl -n "${NAMESPACE}" patch ps "${test_name}" --type=merge \
          -p '{"spec":{"mysql":{"errantTransactionsPolicy":"rebuild"}}}'

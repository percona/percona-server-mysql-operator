apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 150
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      wait_for_same_primary_from_haproxy "${test_name}" >/dev/null

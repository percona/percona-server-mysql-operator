apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |-
      set -o errexit
      set -o xtrace

      source ../../functions

      get_cr "single-node" \
          | yq eval '.spec.mysql.clusterType="group-replication"' - \
          | yq eval '.spec.proxy.router.enabled=false' - \
          | yq eval '.spec.proxy.haproxy.enabled=true' - \
          | yq eval '.spec.mysql.bootstrap.mode="manual"' - \
          | yq eval '.spec.unsafeFlags.proxySize=true' - \
          | yq eval '.spec.unsafeFlags.mysqlSize=true' - \
          | yq eval '.spec.proxy.haproxy.size=1' - \
          | yq eval '.spec.mysql.size=1' - \
          | yq eval '.spec.secretsName="test-secrets"' - \
          | yq eval '.spec.mysql.resources.limits.memory="4Gi"' - \
          | yq eval '.spec.mysql.resources.requests.memory="2Gi"' - \
          | yq eval '.spec.mysql.resources.requests.cpu="500m"' - \
          | yq eval '.spec.mysql.volumeSpec.persistentVolumeClaim.resources.requests.storage="12Gi"' - \
          | kubectl -n "${NAMESPACE}" apply -f -
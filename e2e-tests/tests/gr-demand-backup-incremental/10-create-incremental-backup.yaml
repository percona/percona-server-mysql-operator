apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - timeout: 30
    script: |
      set -o errexit
      set -o xtrace

      source ../../functions

      run_backup incr-3 "$(get_default_storage_name)" "$(get_cluster_name)" "incremental"
